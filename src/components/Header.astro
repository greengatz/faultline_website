---
import { Image } from 'astro:assets';
import logoIcon from '../assets/maelstrom-dice_2x.png';

const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.replace(/\/$/, ""); // Normalize trailing slashes

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, "");

const navItems = [
  { href: `${baseUrl}/`, label: "Welcome" },
  { href: `${baseUrl}/who-we-are`, label: "What is Faultline" },
  { href: `${baseUrl}/join-us`, label: "Join Us" }
];
---

<header class="header">
  <div class="container">
    <a href={`${baseUrl}/`} class="logo">
      <Image src={logoIcon} alt="Faultline 40k Logo" class="logo-icon" width={42} height={42} />
      FAULTLINE <span class="logo-accent">40K</span>
    </a>
    <nav class="nav">
      {navItems.map((item) => {
        const isActive = currentPath === item.href.replace(/\/$/, "");
        return (
          <a
            href={item.href}
            class={`nav-link ${isActive ? "active" : ""}`}
          >
            {item.label}
          </a>
        );
      })}
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background-color: rgba(30, 30, 35, 0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    height: 70px;
    display: flex;
    align-items: center;
  }

  .container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 2.1rem;
    font-weight: 700;
    text-decoration: none;
    color: #ffffff;
    letter-spacing: -0.025em;
    line-height: 1;
  }

  .logo-icon {
    display: block;
    object-fit: contain;
  }

  .logo-accent {
    color: #3ea9f5;
  }

  .nav {
    display: flex;
    gap: 2rem;
  }

  .nav-link {
    color: #a1a1aa;
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: color 0.2s ease, transform 0.2s ease;
    position: relative;
    padding: 0.25rem 0;
  }

  .nav-link:hover {
    color: #ffffff;
  }

  .nav-link.active {
    color: #3ea9f5;
  }

  .nav-link.active::after {
    content: "";
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 2px;
    background-color: #3ea9f5;
    border-radius: 2px;
  }

  @media (max-width: 640px) {
    .container {
      padding: 0 1rem;
    }
    .nav {
      gap: 1rem;
    }
    .nav-link {
      font-size: 0.9rem;
    }
  }
</style>
